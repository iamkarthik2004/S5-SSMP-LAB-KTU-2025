DATA SEGMENT
STR DB 'HELLO$',0
LEN DB ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
    MOV AX,DATA
    MOV DS,AX

    LEA SI,STR
    MOV CL,00H

L1: MOV AL,[SI]
    CMP AL,'$'
    JE DONE
    INC CL
    INC SI
    JMP L1

DONE:
    MOV LEN,CL
    INT 3

CODE ENDS
END START
